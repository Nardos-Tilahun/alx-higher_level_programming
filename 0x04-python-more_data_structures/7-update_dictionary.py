#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    if key not in a_dictionary:
        abc[key] = v
    else:
        for i in abc:
            if i == key:
                abc[i] = v
    return abc
